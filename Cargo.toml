[package]
name = "okf"
version = "0.1.0-alpha.1"
edition = "2024"
description = "A Rust SDK for Open Knowledge Format (OKF)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
