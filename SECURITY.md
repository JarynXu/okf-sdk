# Security policy

Do not report security vulnerabilities in public issues.

Until a dedicated security contact is published, use GitHub's private vulnerability reporting
feature for this repository. Include affected versions, reproduction steps, and the expected
impact.

The parser treats bundle content as untrusted input. It does not execute Markdown, follow links
outside the selected root, or load non-Markdown files by default.
